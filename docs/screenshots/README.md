# Screenshots

Drop PNG screenshots here with these exact names so they render in the main README:

- `discover.png` — the Discover tab (genre/mood chips + recommendation cards)
- `rate.png` — the Rate tab (poster grid with thumb buttons)
- `watchlist.png` — the Watchlist (Want-to-Watch list)
- `chat.png` — the Chat tab

Tip: a clean ~1400px-wide browser capture of each tab looks best.
